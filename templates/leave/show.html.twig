{% extends 'base.html.twig' %}

{% block title %}Leave{% endblock %}

{% block body %}
    <h1>Leave</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leave.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ leave.type }}</td>
            </tr>
            <tr>
                <th>FromDate</th>
                <td>{{ leave.fromDate ? leave.fromDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ToDate</th>
                <td>{{ leave.toDate ? leave.toDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_leave_index') }}">back to list</a>

    <a href="{{ path('app_leave_edit', {'id': leave.id}) }}">edit</a>

    {{ include('leave/_delete_form.html.twig') }}
{% endblock %}
