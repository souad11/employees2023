{% extends 'base.html.twig' %}

{% block title %}New Demand{% endblock %}

{% block body %}
    <h1>Create new Demand</h1>

    {{ include('demand/_form.html.twig') }}

    <a href="{{ path('app_employee_show', {'id': app.user.id}) }}">back to profil</a>

    {% for message in app.flashes('danger') %}
        <div class="alert alert-danger" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endblock %}
